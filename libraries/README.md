# Libraries

Shared Pine helper logic can live here later if multiple indicators clearly need the same reusable code.

Do not extract logic into libraries until the reuse need is obvious and intentional.
