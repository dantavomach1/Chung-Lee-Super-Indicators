# Naming Rules

- Indicator folders use lowercase-with-hyphens.
- Pine files use the `.pine` extension.
- `original.pine` is the untouched import copy.
- `source.pine` is the editable working copy.
- Published files use the format `indicator-name_COMPILED.pine`.
- `changelog.md` and `notes.md` belong inside each indicator folder.
